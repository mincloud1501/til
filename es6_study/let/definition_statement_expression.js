(function() {
    'use strict';

    // definition
    let x = 10;
    let y = 20;

    // statement
    /*
    let (x = x * 2, y = 30) {
        console.log((x + y) + " == 50");
    }
    */

    console.log((x + y) + " == 30");

    // expression
    // console.log(let (x = 100) x + "== 100");
    console.log(x + " == 10");
}());
